# StoreKit configuration (simulator only)

`Vittio.storekit` mirrors the two App Store subscriptions so the paywall renders
with prices in the simulator, without a device or a sandbox account.

It exists because of a circular dependency: App Store Connect will not move a
subscription past **Missing Metadata** until you upload a Review Information
screenshot of the paywall, and StoreKit will not vend a product that is still in
Missing Metadata — so the paywall has no prices to screenshot. A local StoreKit
config breaks the loop.

Product IDs, group id and prices match App Store Connect. Keep them in sync;
the point of this file is to look like production, not to invent a catalog.

## Using it

```bash
npx expo run:ios          # dev build — react-native-purchases is native, Expo Go cannot load it
```

Then in Xcode: **Product → Scheme → Edit Scheme → Run → Options →
StoreKit Configuration → Vittio.storekit**.

Set the storefront to **Mexico** in the same Options pane if the prices render in
the wrong currency — `_storefront` in this file is a hint, and the scheme wins.

## What this does and does not prove

**Does:** the paywall renders, plan cards show store-formatted prices, layout and
copy are right. Good enough for the App Review screenshot.

**Does not:** anything past the purchase sheet. Receipts from a local StoreKit
config cannot be validated by Apple, so RevenueCat never sees the purchase, the
webhook never fires, and `ApplePremiumSubscription` is never written. The whole
server-side chain is untested here.

Verify that end to end on a **physical device via TestFlight with a Sandbox
tester**, before every submission that touches purchase code.

## Caveat after `expo prebuild`

The scheme setting lives in `ios/`, which is gitignored and regenerated by CNG.
Re-select the StoreKit configuration after any prebuild — the file survives, the
scheme's pointer to it does not.
