# Comment-triggered workflows use the workflow file from the repo default branch until merged.
name: Mobile E2E

on:
  issue_comment:
    types: [created]
  pull_request:
    types: [closed]

jobs:
  e2e:
    # GitHub Expressions have no lowercase(); keep trigger phrase exact (also matches as substring).
    if: github.event_name == 'issue_comment' && github.event.issue.pull_request && contains(github.event.comment.body || '', 'run-e2e')
    runs-on: ubuntu-latest
    permissions:
      checks: write
      contents: write
      issues: write
      pull-requests: write

    env:
      EXPO_PUBLIC_API_URL: http://localhost:3001/api/v1

    steps:
      - name: Create PR check run
        id: create_check
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = context.payload.issue.number;
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`;
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: issueNumber
            });
            const { data: check } = await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: "Mobile E2E (run-e2e)",
              head_sha: pr.head.sha,
              status: "in_progress",
              details_url: runUrl,
              output: {
                title: "Mobile E2E started",
                summary: "Triggered by PR comment `run-e2e`."
              }
            });
            core.setOutput("check_run_id", String(check.id));
            core.setOutput("head_sha", pr.head.sha);

      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: refs/pull/${{ github.event.issue.number }}/head

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build Expo web output
        run: npm run e2e:build

      - name: Install Playwright browser
        run: npx playwright install chromium --with-deps

      - name: Run Playwright E2E
        id: playwright
        continue-on-error: true
        run: npm run e2e

      - name: Upload Playwright report
        id: upload_report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-e2e-playwright-report
          path: e2e/playwright-report
          if-no-files-found: ignore
          retention-days: 3

      - name: Upload Playwright screenshots and videos
        id: upload_evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-e2e-test-results
          path: e2e/test-results
          if-no-files-found: ignore
          retention-days: 3

      - name: Publish report to GitHub Pages (per PR)
        id: publish_pages
        if: always()
        env:
          PR_NUMBER: ${{ github.event.issue.number }}
          REPOSITORY_OWNER: ${{ github.repository_owner }}
          REPOSITORY_NAME: ${{ github.event.repository.name }}
        run: |
          set -euo pipefail
          REPORT_DIR="${GITHUB_WORKSPACE}/e2e/playwright-report"
          if [ ! -f "${REPORT_DIR}/index.html" ]; then
            echo "report_url=" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          PAGES_DIR="$(mktemp -d)"
          git fetch origin gh-pages || true
          if git show-ref --verify --quiet refs/remotes/origin/gh-pages; then
            git worktree add "${PAGES_DIR}" origin/gh-pages
            cd "${PAGES_DIR}"
            git checkout -B gh-pages
          else
            git worktree add --detach "${PAGES_DIR}"
            cd "${PAGES_DIR}"
            git checkout --orphan gh-pages
            git rm -rf . >/dev/null 2>&1 || true
          fi

          TARGET_DIR="mobile-e2e/pr-${PR_NUMBER}"
          rm -rf "${TARGET_DIR}"
          mkdir -p "${TARGET_DIR}"
          cp -R "${REPORT_DIR}/." "${TARGET_DIR}/"
          touch .nojekyll

          git add "${TARGET_DIR}" .nojekyll
          if git diff --cached --quiet; then
            echo "No report changes to publish."
          else
            GIT_AUTHOR_NAME=github-actions \
            GIT_AUTHOR_EMAIL=github-actions@github.com \
            GIT_COMMITTER_NAME=github-actions \
            GIT_COMMITTER_EMAIL=github-actions@github.com \
            git commit -m "Publish Mobile E2E report for PR #${PR_NUMBER}"
            git push origin gh-pages
          fi

          OWNER_LOWER="$(printf '%s' "${REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
          echo "report_url=https://${OWNER_LOWER}.github.io/${REPOSITORY_NAME}/${TARGET_DIR}/index.html" >> "${GITHUB_OUTPUT}"

      - name: Comment E2E results on PR
        if: always()
        uses: actions/github-script@v7
        env:
          PLAYWRIGHT_OUTCOME: ${{ steps.playwright.outcome }}
          PAGES_REPORT_URL: ${{ steps.publish_pages.outputs.report_url }}
          REPORT_ARTIFACT_ID: ${{ steps.upload_report.outputs['artifact-id'] }}
          REPORT_ARTIFACT_URL: ${{ steps.upload_report.outputs['artifact-url'] }}
          EVIDENCE_ARTIFACT_ID: ${{ steps.upload_evidence.outputs['artifact-id'] }}
          EVIDENCE_ARTIFACT_URL: ${{ steps.upload_evidence.outputs['artifact-url'] }}
          RESULTS_JSON: e2e/playwright-report/results.json
        with:
          script: |
            const fs = require("node:fs");
            const marker = "<!-- mobile-e2e-comment -->";
            const issueNumber = context.payload.issue.number;
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`;
            const status = process.env.PLAYWRIGHT_OUTCOME === "success" ? "passed" : "failed";
            const icon = process.env.PLAYWRIGHT_OUTCOME === "success" ? "✅" : "❌";
            const hostedReportUrl = process.env.PAGES_REPORT_URL || "";
            const reportLink =
              process.env.REPORT_ARTIFACT_URL ||
              (process.env.REPORT_ARTIFACT_ID
                ? `${runUrl}/artifacts/${process.env.REPORT_ARTIFACT_ID}`
                : runUrl);
            const evidenceLink =
              process.env.EVIDENCE_ARTIFACT_URL ||
              (process.env.EVIDENCE_ARTIFACT_ID
                ? `${runUrl}/artifacts/${process.env.EVIDENCE_ARTIFACT_ID}`
                : runUrl);
            const failedTests = [];

            if (fs.existsSync(process.env.RESULTS_JSON || "")) {
              const report = JSON.parse(fs.readFileSync(process.env.RESULTS_JSON, "utf8"));
              const collectFailedTests = (suite, titlePath = []) => {
                const suiteTitlePath = suite.title ? [...titlePath, suite.title] : titlePath;
                for (const spec of suite.specs || []) {
                  const hasFailure = (spec.tests || []).some((test) =>
                    (test.results || []).some((result) =>
                      ["failed", "timedOut", "interrupted"].includes(result.status)
                    )
                  );
                  if (hasFailure) {
                    failedTests.push([...suiteTitlePath, spec.title].filter(Boolean).join(" › "));
                  }
                }
                for (const childSuite of suite.suites || []) {
                  collectFailedTests(childSuite, suiteTitlePath);
                }
              };
              collectFailedTests(report);
            }
            const failedSummary =
              failedTests.length > 0
                ? `\n### Failed tests\n${failedTests.slice(0, 10).map((name) => `- \`${name}\``).join("\n")}${
                    failedTests.length > 10 ? `\n- ...and ${failedTests.length - 10} more` : ""
                  }\n`
                : "";
            const body = `${marker}
            ## ${icon} Mobile Playwright E2E ${status}

            - Workflow run: [Open run logs](${runUrl})
            - Hosted HTML report: ${hostedReportUrl ? `[Open report](${hostedReportUrl})` : "Not available for this run"}
            - Artifact fallback (optional download): [Report ZIP](${reportLink}) | [Screenshots/videos ZIP](${evidenceLink})
            ${failedSummary}

            Triggered by comment: \`run-e2e\``;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              per_page: 100
            });
            const existing = comments.find((comment) =>
              comment.user?.type === "Bot" && comment.body?.includes(marker)
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body
              });
            }

      - name: Update PR check run
        if: always()
        uses: actions/github-script@v7
        env:
          PLAYWRIGHT_OUTCOME: ${{ steps.playwright.outcome }}
          CHECK_RUN_ID: ${{ steps.create_check.outputs.check_run_id }}
        with:
          script: |
            if (!process.env.CHECK_RUN_ID) {
              core.warning("No check run id found; skipping check update.");
              return;
            }
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`;
            const succeeded = process.env.PLAYWRIGHT_OUTCOME === "success";
            await github.rest.checks.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              check_run_id: Number(process.env.CHECK_RUN_ID),
              status: "completed",
              conclusion: succeeded ? "success" : "failure",
              completed_at: new Date().toISOString(),
              details_url: runUrl,
              output: {
                title: succeeded ? "Mobile E2E passed" : "Mobile E2E failed",
                summary: succeeded
                  ? "Playwright E2E completed successfully."
                  : "Playwright E2E failed. Check workflow logs and artifacts."
              }
            });

      - name: Fail workflow when Playwright fails
        if: steps.playwright.outcome != 'success'
        run: exit 1

  cleanup_pages:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Remove PR report folder from GitHub Pages
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail
          if ! git ls-remote --exit-code --heads origin gh-pages >/dev/null 2>&1; then
            echo "gh-pages branch does not exist, nothing to clean."
            exit 0
          fi

          PAGES_DIR="$(mktemp -d)"
          git fetch origin gh-pages
          git worktree add "${PAGES_DIR}" origin/gh-pages
          cd "${PAGES_DIR}"
          git checkout -B gh-pages

          TARGET_DIR="mobile-e2e/pr-${PR_NUMBER}"
          if [ ! -d "${TARGET_DIR}" ]; then
            echo "No report folder for PR ${PR_NUMBER}, nothing to clean."
            exit 0
          fi

          rm -rf "${TARGET_DIR}"
          git add -A
          if git diff --cached --quiet; then
            echo "No cleanup changes detected."
            exit 0
          fi

          GIT_AUTHOR_NAME=github-actions \
          GIT_AUTHOR_EMAIL=github-actions@github.com \
          GIT_COMMITTER_NAME=github-actions \
          GIT_COMMITTER_EMAIL=github-actions@github.com \
          git commit -m "Cleanup Mobile E2E report for closed PR #${PR_NUMBER}"
          git push origin gh-pages
